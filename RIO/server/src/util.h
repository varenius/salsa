#ifndef UTIL_H_INCLUDED
#define UTIL_H_INCLUDED

/* replaces leading spaces with zeros */
char *
lstrip(char *s);

/* replaces trailing spaces with zeros*/
char *
rstrip(char *s);

/* replaces leading and trailing spaces with zeros*/
char *
strip(char* s);

#endif /* UTIL_H_INCLUDED */
